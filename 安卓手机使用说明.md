# 《风云战机》安卓单文件版

## 发送到手机

需要发送的文件只有一个：

`dist/风云战机-安卓单文件版.html`

可以通过数据线、网盘、QQ、微信文件或局域网传输。部分聊天软件会拦截 HTML 文件；遇到这种情况，可以先把它压缩成 ZIP，发送后在手机上解压。

## 在安卓手机打开

1. 在手机的“文件管理”中找到下载的 HTML 文件。
2. 点击文件并选择浏览器打开。推荐 Chrome、Edge、三星浏览器或其他支持本地 HTML 的浏览器。
3. 进入游戏后使用手指拖动战机，点击屏幕按钮释放技能。

该文件已经内嵌全部运行脚本、样式和战机图片，不需要联网，也不需要电脑开启服务器。

## 存档注意事项

游戏会尝试把进度保存在打开它的浏览器中。不同安卓浏览器对本地文件存档的支持不完全一致，因此建议：

- 始终使用同一个浏览器打开；
- 不要频繁更改文件名或移动文件位置；
- 不要清除该浏览器的数据；
- 重要进度仍建议保留电脑上的原项目。

## 重新生成手机版

电脑项目更新后，在项目目录执行：

```powershell
npm run build:mobile
```

新的单文件版会生成到 `dist` 文件夹。
